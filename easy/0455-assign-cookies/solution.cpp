
            if(g[right]<= s[left]){
                right++;
         while(left< m  &&  right <n){
         
         sort(s.begin(),s.end());

            }
left++;

         sort(g.begin(),g.end());
int right = 0;
int left = 0;

         int m =  s.size();
         }

    }
         int n =  g.size(); 
    int findContentChildren(vector<int>& g, vector<int>& s) {
public:
class Solution {
         return right;
         
};
